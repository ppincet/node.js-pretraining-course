---
topic: "useEffect Hook"
taskNumber: 8
---

## Task 8: Fetch ToDos from API

**Description:**  
Use `useEffect` to fetch todos from an API endpoint (e.g., `/api/todos`) when the component mounts and display them.

**Example:**
```jsx
// On mount, fetch and display todos from /api/todos
``` 
---
topic: "Docker Environment Variables"
taskNumber: 4
---

## Task 4: Pass NODE_ENV in Dockerfile

**Description:**  
Set an environment variable `NODE_ENV=production` in a Dockerfile for the ToDo app.

**Example:**
```dockerfile
# Should set NODE_ENV to production
``` 